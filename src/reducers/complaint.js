import { actionTypes } from "../constants/complaint";


const reducers = (state = INITIAL_STATE, action) => {
    switch(action.type) {
        case actionTypes.REGISTER_COMPLAINT:
            return {}
    }
}

